#include <iostream>
using namespace std;
int main() {
    int t;
    cin >> t;
    while(t--){
        int b,a[b],n=0;
        cin >> b;
        for(int i=0;i<b;i++){
            cin >> a[i];
            if(a[i]>=1000)
            n++;
        }
        cout << n << endl;
    }
 
    return 0;
}
