#include <iostream>
using namespace std;

int main(){
	int a,arr[a];
	cin >> a;
	int c=0;
	for(int i=0;i<a;i++){
		cin >> arr[i];
		if(i==0 || arr[i]>c)
		c=arr[i];
	}
	cout <<"The Greatest In The Array Is " << c << "\n";
}
